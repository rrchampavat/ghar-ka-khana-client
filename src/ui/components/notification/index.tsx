import { Notification as MantineNotification } from "@mantine/core";

const Notification = () => {
  return (
    <MantineNotification title="This is supposed to be a title!">
      This is a Notification.
    </MantineNotification>
  );
};

export default Notification;
